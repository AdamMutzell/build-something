apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.37.0 (HEAD)
  labels:
    io.kompose.service: link-provider
  name: link-provider
spec:
  ports:
    - name: "3000"
      port: 3000
      targetPort: 5000
  selector:
    io.kompose.service: link-provider
